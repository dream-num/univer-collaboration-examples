# Examples 维护约束

- 六个示例保持相同的 `web/main.ts + server/main.ts` 最小骨架。
- 每个示例只回答一个用户问题，不注册与主题无关的高级 Sheet 插件。
- 示例彼此不导入业务源码，允许少量重复以保持单目录可读和可复制。
- 每个入口按实际执行顺序展开，不为教学 demo 增加工厂、分层接口或兜底抽象。
- 固定身份、固定授权和演示密钥必须明确标记为非生产配置。
- 所有示例从 package 公共根入口导入，不依赖仓库内部源码。
- README、测试和启动命令必须与当前实现同步。
- 产品纵切面和能力展示不进入 `examples`。
